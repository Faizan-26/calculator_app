import 'package:flutter/material.dart';

class TempScreen extends StatelessWidget {
  const TempScreen({super.key, required this.appbarName});
  final String appbarName;
  @override
  Widget build(BuildContext context) {
    print("Temp Screen");
    return Scaffold(
        // appBar: a,
        );
  }
}
